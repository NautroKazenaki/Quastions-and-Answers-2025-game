# Инструкция по настройке и запуску

## ⚠️ Важно: Перезапуск dev-сервера

После установки всех зависимостей необходимо **перезапустить dev-сервер**:

1. Если сервер запущен - остановите его (Ctrl+C в терминале)
2. Запустите заново:
```bash
npm run dev
```

## 🚀 Быстрый старт

### 1. Первый запуск

```bash
# Установка зависимостей (уже выполнено)
npm install

# Запуск dev-сервера
npm run dev
```

После запуска откройте в браузере: http://localhost:5173/

### 2. Редактирование вопросов

Откройте файл `src/data/questions.json` и замените примеры вопросов на свои.

**Структура:**
- Первые 5 тем (round: 1) - вопросы от 100 до 500 очков
- Последние 5 тем (round: 2) - вопросы от 200 до 1000 очков

### 3. Сборка для деплоя

```bash
# Создание production-сборки
npm run build

# Проверка production-сборки локально
npm run preview
```

## 🌐 Деплой на Vercel

### Способ 1: Через командную строку (рекомендуется)

```bash
# Установка Vercel CLI
npm install -g vercel

# Деплой
vercel

# Или сразу в production
vercel --prod
```

### Способ 2: Через GitHub и Vercel Dashboard

1. **Создайте репозиторий на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit: New Year Quiz"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

2. **Подключите к Vercel:**
   - Зайдите на https://vercel.com
   - Нажмите "New Project"
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит настройки Vite
   - Нажмите "Deploy"

3. **Готово!** Ваше приложение будет доступно по адресу типа: `https://your-project.vercel.app`

## 🎮 Как использовать приложение

### Подготовка к игре:
1. Откройте приложение на большом экране (для всех участников)
2. Убедитесь, что все вопросы заполнены
3. Объясните правила участникам

### Процесс игры:
1. **Выбор игрока:** Кликните на карточку игрока внизу экрана
2. **Выбор вопроса:** Активный игрок выбирает тему и номинал
3. **Вопрос открывается:** Зачитайте вопрос вслух
4. **Таймер:** Нажмите "Старт таймера" для отсчета 15 секунд
5. **Ответ игрока:** 
   - Если правильно → нажмите "+N" у имени игрока
   - Если неправильно → нажмите "-N"
   - Можно дать шанс другому → кликните на его имя
6. **Закрытие:** Нажмите "Закрыть вопрос" для возврата к таблице

### Особенности:
- **Прогресс сохраняется** автоматически в браузере
- **Кнопка "Сброс игры"** - начать заново (данные удалятся!)
- **Два раунда** - второй раунд дороже для камбэка

## 🐛 Решение проблем

### Проблема: "Module not found" или похожие ошибки
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Tailwind стили не применяются
```bash
# Убедитесь, что установлена версия 3.x
npm install -D tailwindcss@^3 postcss@^8 autoprefixer@^10

# Перезапустите dev-сервер
```

### Проблема: TypeScript ошибки при сборке
```bash
# Проверьте tsconfig и пересоберите
npm run build
```

## 📦 Проверка готовности к деплою

Перед деплоем убедитесь:
- ✅ `npm run build` выполняется без ошибок
- ✅ `npm run preview` показывает рабочее приложение
- ✅ Все вопросы заполнены в `questions.json`
- ✅ Файлы `.gitignore` и `vercel.json` на месте

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте терминал на ошибки
3. Перезапустите dev-сервер
4. Пересоберите проект: `npm run build`

---

**Приятной игры и с Новым Годом!** 🎉

